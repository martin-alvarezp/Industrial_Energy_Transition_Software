# API HTTP thin (SPEC §12) — prompt posterior: expondrá run_scenario como JSON
# según docs/api_contract.md. El motor ya es invocable directo:
#   run_scenario("data/sample_sites/demo", "emissions_cap")
